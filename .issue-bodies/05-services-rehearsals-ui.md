Service planning + rehearsal workflow frontend from the batch. The SQL-side defects (no write path to `services`, overlap vacuous on NULL budgets, `create_rehearsal` unique violation, RSVP grant gap) are tracked in issue #1 (migration/RLS checklist).

## Checklist

- [ ] **[medium]** `src/pages/ServiceDetail.jsx:404-411, 516` — `setWarningsChecked(true)` runs **before** the validate RPC; on failure only `topError` is set while `warnings` stays `[]`, so the warnings section renders "No warnings — the plan looks good." alongside the error. Trigger: leader clicks "Validate plan" while the RPC fails (offline/RLS). Related: `warnings`/`warningsChecked` are never invalidated when blocks/assignments change via `load()`, so warnings displayed after an edit describe the **previous** plan state.
- [ ] **[low]** `src/pages/ServiceDetail.jsx:46-49, 551` — the call sheet dereferences `block.startOffsetMinutes` whenever `service.startsAt` is set, but it receives `blocks.find(...)` which the same JSX acknowledges may be `undefined` (`block?.name` two lines above). Trigger: start time set + assignment whose block row is absent (blocks and assignments are fetched in parallel; block deleted in between) → `TypeError` during render → blank page.
- [ ] **[low]** `src/pages/ServiceDetail.jsx:164` — "Assigning:" reads `selected.displayName || selected.username`, but members from `resolveMemberNames` are `{id, name}` → renders a bare `Assigning: `. Trigger: pick someone from the "Known members…" select instead of search results.
- [ ] **[low]** `src/pages/Services.jsx:131-135` — `handleCreated` calls `setShowForm(false)` **before** `await refresh()`; if the refresh rejects, the error lands in the unmounted form's `setErrors({form})` → no message anywhere and a stale list. Trigger: create a service while the follow-up fetch fails.
- [ ] **[low]** `src/pages/ServiceDetail.jsx:374-380` / `src/pages/RehearsalDetail.jsx:43-52` — neither effect clears `data` when the route `id` changes (RehearsalDetail also keeps `notice`/`draftNotes`). Trigger: browser back/forward directly between two detail URLs → one frame of the old record; if the new fetch **fails**, the previous service/rehearsal stays on screen under the new URL with only an error banner.
- [ ] **[low]** `src/pages/RehearsalDetail.jsx:348` — carry-over heuristic `Boolean(item.notes) && !item.outcome` flags **any** noted-but-not-outcomed item with the red "Carried over: needs work" badge. Trigger: type a rehearsal note during a live session without picking an outcome → red badge implying carry-over from a previous rehearsal.
- [ ] **[low]** `src/lib/services.js:203-207` — `getService` uses `select('*')` without the org/branch embeds, so `service.orgName`/`branchName` are always `null` on the detail page while the list page (which embeds `organizations(name)`) shows the name → detail header renders `—`. Orgs are readable (0016), so this is an inconsistency, not an RLS artifact.
