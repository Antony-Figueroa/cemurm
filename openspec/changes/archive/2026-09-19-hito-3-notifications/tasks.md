# Tasks: Hito 3 — Notifications feed + @mention

Change 2 of Hito 3 (`hito-3-notifications`), built on the archived `hito-3-band-collaboration` (change 1, `f8c1860`). DB-backed trigger-emitted notifications (exploration A1 / proposal A1): migrations **0008 core** (`0008_notifications_core.sql`, PR#0) + **0009 activity** (`0009_notifications_activity.sql`, PR#1) — together the original single-file 0008 (8 trigger functions across 4 tables — 2+3+2+1 — plus 2 security-definer write RPCs, realtime publication + `replica identity full`), then the client data layer (`src/utils/relativeTime.js` + `src/lib/notifications.js`, PR#2a), the UI shell (hook + `/notifications` route + page + AppLayout badge, PR#2b), read-state via the existing 0002 grants, offline badge/feed cache without any schema bump. 22 IN scenarios; Web Push and every `notification_preferences` surface stay OUT. No test runner in this workspace (`strict_tdd: false`): validation = `demo()` self-checks, psql dry-run walk, manual two-identity walk, `pnpm lint` / `pnpm build`.

## Resolved Design Questions (decisions)

1. **Removed-user title = spec literal, no actor prefix**: `"You have been removed from {Setlist}"` (notifications S4 + shared-setlist S10 wording). Reconciles design line 285 draft (`{Actor} removed you from {Setlist}`) vs the emission table (row 5) and the spec literal: the feature literal is the intended form. Task 0.4, asserted in dry-run.
2. **`shared_comments` = ONE trigger function** covering both comment-recipient rows and @mention rows. Trigger count is exactly **2+3+2+1 = 8** (bandmate_links ×2 in 0008 core, setlist_collaborators ×3 in 0008 core, setlist_items ×2 in 0009, shared_comments ×1 in 0009); design line 115's "shared_comments INSERT + @mention" is one function, not two. Asserted in 1.5 (pg_trigger breakdown).
3. **Interim window is bounded**: between PR#1 and PR#4 the client still runs the old per-song reorder loop → the statement-level trigger emits N rows per reorder (documented over-emission; the client-side no-op guard lands with the RPC swap). The two `src/lib/setlists.js` RPC swaps are the FIRST tasks of PR#4 (4.1, 4.2); manual reorder verification in PR#4 exercises the RPC path only.
4. **Chain strategy resolved (feature-branch-chain)**: design recommends feature-branch-chain; chain as re-sliced per RQ #8 (PR#0 base = tracker branch `feat/hito-3-notifications`; PR#1 base = PR#0 branch; PR#2a base = PR#1 branch; PR#2b base = PR#2a branch; PR#4 base = PR#2b branch; PR#5 base = PR#4 branch; PR#3 intentionally does not exist; only the tracker merges to main). Owner confirmed 2026-09-18 (strategy + re-slice); `delivery_strategy` cached = `auto-chain` → no decision needed before apply.
5. **Threat matrix all N/A**: this change adds a client React route, DB triggers, and two PostgREST RPCs — no routing/shell/subprocess/VCS-executable boundary. No RED security tests are manufactured; security-relevant invariants are covered by dry-run psql assertions (42501 locks, scope rejections, publication RLS-only).
6. **Apply-time open questions (not tasks)**: (a) mention regex (`@[A-Za-z0-9._-]+` assumed) must match the username charset enforced at signup in `src/lib/auth.js` (align during apply, keep the same character class as `profiles.username` creation); (b) confirm the local Realtime stack honors the non-PK `user_id` filter with `replica identity full` — the focus-refetch fallback is already designed into 2.3/4.6 if verification shows failure.
7. **Owner decision 2026-09-18 — migration split, no size:exception**: the original single-file `0008_notifications.sql` (501 authored lines) was split by owner order into TWO migrations — `0008_notifications_core.sql` (PR#0, 249 lines: shared helpers, bandmate_links + setlist_collaborators emissions, transfer RPC) and `0009_notifications_activity.sql` (PR#1, ~300 lines: move RPC + statement-level reorder, song-added, shared_comments, publication + replica identity). Fixed 400-line PR budget enforced for every slice; `size:exception` was REJECTED (see apply-progress "first pass" note).
8. **Owner decision 2026-09-18 — PR#2 bipartido, no size:exception**: the first-pass PR#2 slice (5 commits `9910754`→`45689eb`, 633 changed lines) had `size:exception` REJECTED by owner order and was RE-SLICED into **PR#2a** (tasks 2.1–2.2, data layer: `relativeTime.js` + `notifications.js`, incl. the demo assert-count fix 19→24) + **PR#2b** (tasks 2.3–2.5, UI shell: hook + route/page + AppLayout badge). Final chain = **6 PRs: PR#0 → PR#1 → PR#2a → PR#2b → PR#4 → PR#5** — **PR#3 intentionally does not exist (documented gap)**: the former PR#3 (event wiring + read-state + offline, tasks 3.1–3.7) is renumbered **PR#4 (tasks 4.1–4.7)** and the former PR#4 (comments + @mention, tasks 4.1–4.4) is renumbered **PR#5 (tasks 5.1–5.4)**. Fixed 400-line budget applies to every slice; `size:exception` accepted for none.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines (total, all slices) | ~1,260–1,470 (additions + deletions) |
| 400-line budget risk | **High** (total) — chain mandatory; per-slice target ≤400, every slice within budget after the 0008 split AND the PR#2a/PR#2b re-slice (RQ #8) |
| Chained PRs recommended | Yes |
| Suggested split | PR#0 (0008 core migration + dry-run) → PR#1 (0009 activity migration + dry-run) → PR#2a (data layer: relativeTime + notifications.js) → PR#2b (UI shell: hook + route/page/badge) → PR#4 (event wiring + read-state + offline) → PR#5 (comments + @mention) — **PR#3 intentionally skipped** |
| Delivery strategy | auto-chain (owner-cached) |
| Chain strategy | feature-branch-chain (owner-confirmed 2026-09-18) |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

### Per-slice forecast

| Slice | Scope (start → finish) | Est. changed lines | Budget risk |
|-------|------------------------|--------------------|-------------|
| **PR#0** | `supabase/migrations/0008_notifications_core.sql`: shared definer helpers → bandmate_links ×2 + setlist_collaborators ×3 triggers (incl. deferred DELETE) → transfer RPC → dry-run psql walk | ~249 | Low (within budget) |
| **PR#1** | `supabase/migrations/0009_notifications_activity.sql`: move RPC + statement-level reorder trigger → song-added → shared_comments ONE function → publication + `replica identity full` → dry-run psql walk (divided: PR#0 core + PR#1 activity portions) | ~300 | Low (within budget) |
| **PR#2a** | `src/utils/relativeTime.js` → `src/lib/notifications.js` (+ demo; assert count corrected 19→24) — base = pr1 head `aa77884` | ~303 | Low (within budget; verified) |
| **PR#2b** | `src/hooks/useNotifications.js` → `/notifications` route + `src/pages/Notifications.jsx` shell (tabs + empty states) → `src/components/layout/AppLayout.jsx` nav + badge — base = pr2a tip | ~330 | Low (within budget; verified) |
| **PR#4** | `src/lib/setlists.js` RPC swaps (first tasks) → emission verification walk → invitation actions → markRead/markAllRead wiring → IDB cache + reconnect reconcile + delta summary → `src/pages/SetlistDetail.jsx` `?song=` target | ~300–370 | Low–Medium |
| **PR#5** | shared_comments emission verification → `src/pages/SongDetail.jsx` `?anchor=&cid=` target → comment/mention row rendering + mention-scope path checks | ~120–260 | Low |
| **Total** | — | ~1,260–1,470 | **High — 6-PR chain mandatory (PR#3 gap intentional)** |

Apply rule for the ceiling: if any slice's honest authored count lands over 400 during apply, STOP and request `size:exception` for that slice per Section E of `sdd-phase-common.md` — do not silently re-slice. Owner-directed re-slices already executed: the once-atomic 0008 migration was split by owner decision (0008 core + 0009 activity) so both migration slices fit the fixed 400-line budget, and the first-pass PR#2 (633 lines) was re-sliced per RQ #8 into PR#2a (303) + PR#2b (330). Every slice now fits the fixed budget; no `size:exception` was requested or granted.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 0 | 0008 core triggers + transfer RPC | PR#0 | `supabase db reset`, then the divided dry-run psql walk (PR#0 core portion) per `docs/local-dev.md` (read-only) | 2-identity psql session (`supabase/seed.sql` (read-only) identities `demo@cemurm.app` / `isolation@cemurm.app`); `supabase start` live stack | Drop `supabase/migrations/0008_notifications_core.sql` + `supabase db reset` → reverts to 0007; emissions stop; Activity broadcasts untouched |
| 1 | 0009 activity RPC + triggers + publication | PR#1 | `supabase db reset`, then the divided dry-run psql walk (PR#1 activity portion) per `docs/local-dev.md` (read-only) | Same 2-identity psql session; reorder GUC-vs-fallback discriminator assertion on a ≥3-item setlist | Drop `supabase/migrations/0009_notifications_activity.sql` + `supabase db reset` → 0008 core stays; move RPC + reorder/song-added/comment emissions stop; `notifications` leaves the publication |
| 2a | Data layer: relativeTime + notifications.js (+ demo) | PR#2a | `node -e "import('./src/utils/relativeTime.js').then(m=>m.demo())"` && `node -e "import('./src/lib/notifications.js').then(m=>m.demo())"` && `pnpm lint && pnpm build` | Demo-driven (browser-bound UI not yet present — page + badge land in PR#2b) | Revert 2.1–2.2 (`src/utils/relativeTime.js`, `src/lib/notifications.js`) — no DB dependency; IDB cache keys disposable |
| 2b | UI shell: hook + route/page/badge (feed renders empty-state) | PR#2b | `pnpm lint && pnpm build` (UI-only slice — hook/page are UI-bound, no pure demo; the 2a demos still pass in the combined tree) | Dev-browser walk of `/notifications` as `demo@cemurm.app`: tabs, empty states, badge 0 | Revert 2.3–2.5 (`src/hooks/useNotifications.js`, `src/pages/Notifications.jsx`, `src/App.jsx`, `src/components/layout/AppLayout.jsx`) — no DB dependency; IDB cache keys disposable |
| 3 | Event wiring + read-state + offline | PR#4 | `node -e "import('./src/lib/setlists.js').then(m=>m.demo())"` && `pnpm lint && pnpm build` | 2-browser walk (`demo@cemurm.app` ↔ `isolation@cemurm.app`): invite/accept/decline → inviter rows; share/permission/removal; song add (key/tempo); reorder = exactly one row w/ moved-song deep link; transfer = zero "removed"; offline drain + badge + summary | Revert 4.1/4.2 to the legacy request sequences (two functions inside `src/lib/setlists.js`); revert 4.4–4.7 UI wiring; outbox replay falls back to legacy ops |
| 4 | Comments + @mention | PR#5 | `node -e "import('./src/lib/comments.js').then(m=>m.demo())"` (read-only) && `pnpm lint && pnpm build` | 2-browser comment walk: comment → participating bandmates; `@julian.guitar` in-scope → row; `@marco.tempo` out-of-scope → zero rows; deep link anchor+cid | Revert 5.2 (`src/pages/SongDetail.jsx` search-param handling) + 5.3 (comment/mention row rendering in `src/pages/Notifications.jsx`) |

Chain base boundaries (feature-branch-chain, owner-confirmed 2026-09-18; re-slice per RQ #8): PR#0 base = feature/tracker branch; PR#1 base = PR#0 branch; PR#2a base = PR#1 branch; PR#2b base = PR#2a branch; PR#4 base = PR#2b branch; PR#5 base = PR#4 branch. **PR#3 does not exist (intentional gap)**. Only the tracker merges to main. If any child PR diff shows previous slices, retarget/rebase before review. Producers before UI is already enforced by the PR order (0008 core lands in PR#0; 0009 activity in PR#1; PR#2a/PR#2b ship against the populated table; RPC swaps are PR#4's first tasks).

---

## PR#0 — 0008 Core Migration (Phase 1: Foundation / Infrastructure; base = tracker `feat/hito-3-notifications`)
> Verification: `supabase db reset` + divided dry-run psql walk (PR#0 core portion) per `docs/local-dev.md` (read-only); `pnpm lint && pnpm build` smoke (unchanged client). Precedent: archived PR#0 0006 RED-SQL walk.
> Edit target: `supabase/migrations/0008_notifications_core.sql` (new, single file — PR#0 half of the owner-mandated 0008 split, Resolved Decision 7). Contracts read from `supabase/migrations/0001_init.sql`, `0002_rls_core.sql`, `0006_band_collaboration.sql`, `0007_comment_author_writes.sql`, `supabase/seed.sql` (all read-only).

- [x] 0.1 — Create `supabase/migrations/0008_notifications_core.sql`: shared definer helpers `public.notify_user(p_user_id, p_category, p_title, p_body, p_payload)` and `public.notifier_actor_name()` — `security definer set search_path = ''`, fully-qualified `public.` refs, `revoke all on function ... from public, anon, authenticated` (0006 trigger-only precedent). `notify_user` reads `auth.uid()`, suppresses `p_user_id = actor` (self-suppression in one place), injects `payload.actor_id` via `jsonb_set`, inserts into `public.notifications`. `notifier_actor_name` resolves `profiles.display_name → username → 'Someone'` (NULL-tolerant). Both are consumed by 0009 activity (dependency: 0009 applies after 0008). No grants/policies on `notifications` beyond 0002 state. *Est. ~40. Deps: none. Commit `7062c79`.*

- [x] 0.2 — `bandmate_links` triggers ×2 in `supabase/migrations/0008_notifications_core.sql`: (a) AFTER INSERT → `notify_user(new.bandmate_id, 'invitation', "{Actor} invited you to be a bandmate", payload {action:'invite', bandmate_id})`; (b) AFTER UPDATE `old.status='pending'` → `new.status='active'` → `notify_user(new.user_id, 'invitation', "{Actor} accepted your invitation", {action:'bandmate-accepted'})` and `new.status='declined'` → `"{Actor} declined your invitation"`, `{action:'bandmate-declined'}`; actor via `notifier_actor_name`. Category `'invitation'` per the category contract (rows 1–2). *Refs: notifications I1/I2/I3, bandmates S5 ("Carlos receives a notification that I declined"). Est. ~45. Deps: 0.1. Commit `3ea60fa`.*

- [x] 0.3 — `setlist_collaborators` triggers ×2 in `supabase/migrations/0008_notifications_core.sql`: (a) AFTER INSERT → `notify_user(new.user_id, 'setlist', "{Actor} invited you to edit {Setlist}", {action:'shared', setlist_id})`, **skipping when `new.user_id = (select owner_id from setlists where id = new.setlist_id)` at statement time (immediate, NOT deferred — transfer re-insert [Decision 4])**; (b) AFTER UPDATE where `old.can_edit is distinct from new.can_edit` → `notify_user(new.user_id, 'setlist', "Your permissions on {Setlist} have changed to View Only" / "to Edit", {action:'permission', setlist_id})` (the 0006 self-flip case is excluded by `notify_user` self-suppression). *Refs: shared-setlist scenarios, notifications S3. Est. ~50. Deps: 0.1. Commit `1fdad77`.*

- [x] 0.4 — `setlist_collaborators` deferred DELETE trigger in `supabase/migrations/0008_notifications_core.sql`: `create constraint trigger ... after delete on setlist_collaborators deferrable initially deferred for each row` evaluating **at commit**: emit `notify_user(old.user_id, 'setlist', <title>, {action:'removed', setlist_id})` only if the setlist still exists (`ON DELETE CASCADE` from `setlists` already removed it → skip) AND `old.user_id <> (select owner_id from setlists ...)` at commit time (ownership transfer → the deleted user IS the new owner → skip). **Title = spec literal `"You have been removed from {Setlist}"` (S4/S10 wording, no actor prefix — Resolved Decision 1); body NULL.** "No further notifications" holds by row absence, not filter logic. *Refs: notifications S4, setlists S10, shared-setlist removal scenario. Est. ~35. Deps: 0.1. Commit `1fdad77`.*

- [x] 0.5 — Write RPC `public.transfer_setlist_ownership(p_setlist_id uuid, p_new_owner_id uuid)` in `supabase/migrations/0008_notifications_core.sql`: `security definer set search_path = ''`; only the CURRENT owner may call (`private.session_owns_setlist`), else `'No access.'`; re-assert `guardTransfer` semantics with the EXACT existing `USER_ERRORS` messages (`'Only an accepted collaborator can take ownership.'`, `'The new owner must accept the invitation first.'` — `src/lib/setlists.js` lines 21–22 (read-only) so client error mapping passes through); three ops in ONE transaction in this order: (1) DELETE the new owner's `setlist_collaborators` row (deferred trigger evaluates at commit with `owner_id` already flipped → no false "removed"), (2) re-insert the former owner as accepted `can_edit` collaborator (INSERT trigger's immediate owner check skips it), (3) flip `setlists.owner_id`; `revoke`/`grant` per 0002 precedent (authenticated, service_role). *Refs: shared-setlist transfer scenario; zero-'removed' invariant. Est. ~60. Deps: 0.1, 0.3, 0.4. Commit `0d1a34c`.*

> PR#0 verification (divided walk, core portion — ran with the 1.5 walk against 0001–0009): bandmate invite/accept/decline rows, collab invite/permission/removal (spec-literal title, body NULL), transfer zero-'removed', setlist cascade zero-'removed', client 42501 on INSERT/DELETE/non-`read_at` UPDATE, `notification_preferences` zero policies + zero grants, the two `USER_ERRORS` raw messages.

## PR#1 — 0009 Activity Migration (Phase 1: Foundation / Infrastructure — 2nd half of the split; base = `feat/hito-3-notifications-pr0-migration`)
> Verification: `supabase db reset` (0001–0009 in order) + divided dry-run psql walk (PR#1 activity portion) per `docs/local-dev.md` (read-only); `pnpm lint` / `pnpm build` smoke (unchanged client). Precedent: archived 0006 RED-SQL walk + PR#0 core walk.
> Edit target: `supabase/migrations/0009_notifications_activity.sql` (new, single file). SQL bodies derived VERBATIM from the original `0008_notifications.sql` (`git show cefcd2c:supabase/migrations/0008_notifications.sql` — read-only) sections 4 (reorder), 5 (song-added), 7 (shared_comments), 8 (publication + replica identity); header adapted to the split. Contracts read from `supabase/migrations/0001_init.sql`, `0002_rls_core.sql`, `0006_band_collaboration.sql`, `0008_notifications_core.sql`, `supabase/seed.sql` (all read-only).

- [x] 1.1 — Create `supabase/migrations/0009_notifications_activity.sql` (section 1): write RPC `public.move_setlist_items(p_setlist_id uuid, p_ordered_song_ids uuid[], p_moved_song_id uuid)` — `security definer set search_path = ''`; scope re-asserted (definer bypasses RLS): owner (`private.session_owns_setlist`, 0002 line 142 (read-only)) or accepted `can_edit` collaborator, else raw `raise exception 'No access.'`; `perform set_config('cemurm.reorder_moved_song_id', p_moved_song_id::text, true)` (transaction-local GUC); **two-phase protocol REQUIRED by `UNIQUE (setlist_id, position)` (0001 line 188 (read-only))**: pass 1 = single statement bumping all to `10000 + ordinality`, pass 2 = single multi-row statement setting finals `ord - 1`; `revoke all ... from public, anon` + `grant execute ... to authenticated, service_role`. Plus the **statement-level reorder trigger** `setlist_items_reorder_notify` (`notify_setlist_reordered`): `after update ... referencing old table / new table for each statement`; emit exactly ONE row per reorder when `exists(new n join old o on o.id=n.id where n.position is distinct from o.position)` AND `not exists(new_rows where position >= 10000)` (bump pass excluded); moved song = GUC `cemurm.reorder_moved_song_id`, fallback `min(new.position)` (exact for single-row statements); recipients owner + accepted collaborators, `"{Actor} reordered songs in {Setlist}"`, payload `{action:'reorder', setlist_id, song_id}`. Keep the code-level ponytail note: bump offsets / GUC name / protocol must change together (design Decision 2). *Refs: notifications S1, S15. Est. ~96. Deps: PR#0 (helpers). Commit `720a6a8`.*

- [x] 1.2 — `setlist_items` song-added trigger in `supabase/migrations/0009_notifications_activity.sql` (section 2): AFTER INSERT → owner + accepted collaborators of `new.setlist_id` (deduped, minus actor via helper): `"{Actor} added {Song} to {Setlist}"`, payload `{action:'song-added', setlist_id, song_id}`; body appends `"Key: {base_key} · {base_tempo} BPM"` when resolvable via `song_versions` (prefer the item's `version_id`, else latest by `created_at desc` — `src/lib/songs.js` (read-only) precedent), silently omit otherwise (body NULL). *Refs: notifications S2. Est. ~48. Deps: PR#0 (helpers), 1.1. Commit `75ec8dc`.*

- [x] 1.3 — `shared_comments` INSERT trigger **ONE function** (`notify_comment_activity`) in `supabase/migrations/0009_notifications_activity.sql` (section 3) covering BOTH paths (Resolved Decision 2): (a) comment rows → owner + accepted collaborators of ANY setlist containing `new.song_id` (deduped) minus author minus mention recipients: `"{Actor} commented on {Song}"`, payload `{action:'comment', song_id, setlist_id, section, comment_id}`; (b) @mention rows → parse `@username` with the `@[A-Za-z0-9._-]+` regex (align with `src/lib/auth.js` (read-only) username charset — open question, resolved by evidence), resolve against unique `profiles.username`, emit only when the named user passes the **0006 EXISTS scope chain** (setlist_items → setlists owner OR accepted collaborator, nested single-relation EXISTS, no joins — design lines 317–341); out-of-scope mention creates NO row (no un-openable row); self-mention (`candidate = new.author_id`) skipped; title `"@{username} mentioned you in {Song}"`, payload `{action:'mention', song_id, setlist_id, section, comment_id}`; mention recipients excluded from the comment-recipient set (mention row supersedes). Category `'setlist'` (frozen enum). *Refs: comments S11/S12 incl. "no notification for Marco". Est. ~96. Deps: PR#0 (helpers). Commit `386aef5`.*

- [x] 1.4 — Publication + replica identity in `supabase/migrations/0009_notifications_activity.sql` (section 4): `alter publication supabase_realtime add table public.notifications;` and `alter table public.notifications replica identity full;`. **No** `notification_preferences` policy and **no** client INSERT/DELETE grants on `notifications` (0002 locks stay: `select` + `update (read_at)` only, 0002 lines 469–470 (read-only)). *Refs: row-level-security delta scenarios. Est. ~8. Deps: none. Commit `aa77884`.*

- [x] 1.5 — Divided dry-run psql walk (verification task — run assertions in the psql session after `supabase db reset` + seed; **do NOT commit a `.sql` file inside `supabase/migrations/` — db reset executes every `.sql` there; follow the archived 0006 RED-walk + PR#0 walk precedent**). PR#1 activity portion: (a) `pg_trigger` → full 2+3+2+1 across BOTH migrations (setlist_items ×2 + shared_comments ×1 in 0009; shared_comments = ONE function); (b) `pg_proc` → `move_setlist_items` execute granted to authenticated, revoked from public/anon; all 0009 trigger-only functions revoked from public/anon/authenticated; (d) song-added via INSERT → owner + accepted collaborator rows, payload `{song_id, setlist_id}`; reorder via `move_setlist_items` → **exactly ONE row** AND `payload->>'song_id'` = the `p_moved_song_id` passed (GUC-vs-fallback discriminator: move the moved song to position ≠ 0 on a ≥3-item setlist — the `min(new.position)` fallback would name a different song and fail the assertion); reorder no-op → exactly 1 row (documented over-emission; client no-op guard lands with 4.1); (e) comment → owner + accepted collaborators minus author minus mention recipients; `@julian.guitar` in-scope → mention row; `@marco.tempo` out-of-scope → zero rows; self-mention suppressed; mention-recipient excluded from comment set; (g) publication = exactly {setlists, setlist_items, setlist_collaborators, notifications}, all `relrowsecurity` enabled; `notifications` `relreplident = 'f'`; (h) RPC scope rejection (non-editor reorder → raw `'No access.'`); (i) client `INSERT`/`DELETE`/non-`read_at` `UPDATE` on `notifications` as authenticated → 42501; `notification_preferences` zero policies + zero grants. PR#0 core portion re-runs: invite/accept/decline, collab invite/permission/removal, transfer zero-'removed', cascade zero, USER_ERRORS. *Est. ~0 committed lines (session). Deps: 1.1–1.4 + PR#0.*

## PR#2a — Client Data Layer (Phase 2: Core Implementation; base = pr1 head `aa77884` — re-sliced from the original monolithic PR#2 per RQ #8)
> Verification: `node -e "import('./src/utils/relativeTime.js').then(m=>m.demo())"` && `node -e "import('./src/lib/notifications.js').then(m=>m.demo())"`; `pnpm lint && pnpm build`.
> Edit targets: `src/utils/relativeTime.js`, `src/lib/notifications.js`. Read-only refs: `src/lib/offlineCache.js`, `src/lib/supabase.js`, `src/lib/bandmates.js` (lazy-import precedent).

- [x] 2.1 — Create `src/utils/relativeTime.js` (first file in the declared-but-empty `src/utils/`): pure `relativeTime(iso)` → "just now", "5m ago", "2h ago", "3d ago", else date; `demo()` self-checks boundary buckets (0s, 1m, 59m, 1h, 2h, 3d, older). *Refs: Feed1 (relative timestamps). Est. ~45. Deps: none.*

- [x] 2.2 — Create `src/lib/notifications.js`: `listNotifications(userId)` (RLS-capped select, normalized, read-through cache key `notifications:<userId>`), `unreadCount(userId)` (cache key `notifications-unread:<userId>`), `markRead(userId, id)` (`update({read_at: now})` — flows through 0002 `notifications_update_self` + column grant), `markAllRead(userId)` (`update({read_at: now}).is('read_at', null)`), `subscribeNotifications(userId, cb)` (channel `notifications:<userId>`, `postgres_changes` event `'*'`, filter `user_id=eq.<me>`, returns unsubscribe — `subscribeSetlistRealtime` pattern), plus pure helpers `normalizeNotification(row)` (→ `{id, category, title, body, action, target, read, createdAt, actorId}`), `groupByCategory(rows)` (ordered groups + per-group unread counts), `notificationTarget(payload)` (deep-link map → `{route, params|null}`; `null` for informational rows) + `demo()`. Lazy supabase import (bandmates.js precedent). NO INSERT/DELETE calls (0002 grants forbid them — 42501 stays). *Refs: Feed1–7, realtime delivery scenario. Est. ~125. Deps: 2.1.*

> PR#2a verification (2026-09-18): `relativeTime demo OK: 9 asserts` exit 0; **`notifications demo OK: 24 asserts` exit 0** — the demo's printed count was corrected from the stale literal 19 to the actual 24 (`grep -c "assertEq("` = 24: 12 deep-link + 7 normalize + 5 groupByCategory) via commit `1251ca0` `fix(notifications): correct demo assertion count (19→24 actual)`; `pnpm lint` exit 0 (0 warnings); `pnpm build` exit 0 (pre-existing >500 kB chunk warning only). Slice diff vs base `aa77884` = **303 changed lines (303 insertions) ≤ 400 ✓**. Branch `feat/hito-3-notifications-pr2a-data-layer` @ `1251ca0` pushed to origin (`64dcbe5` 2.1 + `0f4decf` 2.2 + `1251ca0` fix). *First pass (superseded): the monolithic PR#2 batch `9910754`→`45689eb` (633 changed lines, size:exception requested) was re-sliced by owner order — see RQ #8 and apply-progress.*

## PR#2b — UI Shell (Phase 2: Core Implementation; base = pr2a tip `1251ca0` — re-sliced from the original monolithic PR#2 per RQ #8; feed renders against the post-0009 table — no producers verified until PR#4)
> Verification: `pnpm lint && pnpm build` (UI-only slice — hook/page are UI-bound, no pure demo; the 2a demos re-run as combined checks on the tip); `node -e ... relativeTime.demo()` + `node -e ... notifications.demo()` combined.
> Edit targets: `src/hooks/useNotifications.js`, `src/pages/Notifications.jsx`, `src/App.jsx`, `src/components/layout/AppLayout.jsx`. Read-only refs: `src/lib/offlineCache.js`, `src/hooks/useAuth.jsx`, `src/hooks/useComments.js`, `src/hooks/useSharedSetlist.js`.

- [x] 2.3 — Create `src/hooks/useNotifications.js`: state `{rows, unreadCount, loading, online, summary, consumeSummary, markRead, markAllRead, refresh}`; reads via 2.2 with IDB read-through (`offlineGet`/`offlineSet` on `src/lib/offlineCache.js` (read-only) — new keys only, no `DB_VERSION`/store change); debounced realtime refetch (150ms, `useSharedSetlist` (read-only) precedent); `online` + `cemurm:sync-done` + `focus` listeners (`useComments` (read-only) precedent); userId from `useAuth` (read-only ref `src/hooks/useAuth.jsx`). Delta-summary synthesis + consume land in 4.6. *Refs: Off1/Off2 surface, realtime scenario. Est. ~95. Deps: 2.2.*

- [x] 2.4 — Add route in `src/App.jsx` (inside the `RequireAuth` block, `Setlists`/`Bandmates` area): `{ path: '/notifications', element: <Notifications /> }` + import. Create `src/pages/Notifications.jsx` shell: category filter tabs **All / Invitations / Setlist Changes / Events / System** (Events + System render empty states until Hito 4 emitters exist — Feed5 contract), grouped rendering with per-group unread counts, relative time via 2.1, per-row read toggle, Mark-all button, empty state, offline state, tap handler routing through `notificationTarget` (navigations fully exercised in PR#4/PR#5 deep-link tasks). Keep this PR lean — invitation action buttons land in 4.4, comment/mention rows in 5.3. *Refs: Feed3–5, Feed1. Est. ~100. Deps: 2.1, 2.2, 2.3.*

- [x] 2.5 — `src/components/layout/AppLayout.jsx`: add `Notifications` nav link with unread badge fed by `useNotifications` (authed only); badge hidden at 0, capped at "99+". *Refs: Feed7. Est. ~35. Deps: 2.3.*

> PR#2b verification (2026-09-18): `pnpm lint` exit 0 (0 warnings); `pnpm build` exit 0 (pre-existing >500 kB chunk warning only); combined re-check on the tip — both 2a demos exit 0 (`9 asserts` + `24 asserts`). Slice diff vs base `1251ca0` = **330 changed lines (329 insertions, 1 deletion) ≤ 400 ✓** (useNotifications 144, Notifications.jsx 161, App.jsx 2, AppLayout.jsx 23/−1). Branch `feat/hito-3-notifications-pr2b-ui-shell` @ `b024872` pushed to origin (`bb2d2ea` 2.3 + `45fcfc5` 2.4 + `b024872` 2.5). The superseded first-pass branch `feat/hito-3-notifications-pr2-data-layer` was DELETED from origin (it never had an open PR).

## PR#4 — Event Wiring + Read-State + Offline (Phase 3: Integration / Wiring; producers verified; renumbered from the former PR#3 per RQ #8 — PR#3 does not exist in the chain)
> Verification: `node -e "import('./src/lib/setlists.js').then(m=>m.demo())"`; two-identity walk per below; `pnpm lint && pnpm build`.
> Edit targets: `src/lib/setlists.js`, `src/pages/Notifications.jsx`, `src/hooks/useNotifications.js`, `src/pages/SetlistDetail.jsx`. Read-only refs: `src/lib/offlineCache.js`, `src/lib/bandmates.js`, `src/lib/setlistCollab.js`, `src/lib/offlineQueue.js`, `src/lib/offlineSync.js`, `docs/local-dev.md`, `supabase/seed.sql`.

- [x] 4.1 — `src/lib/setlists.js` `moveSongInSetlist` (line ~462): replace the two per-song UPDATE loops with a single `supabase.rpc('move_setlist_items', { p_setlist_id, p_ordered_song_ids: reordered, p_moved_song_id: moved })` call; the offline path (`enqueueOp` with name `moveSongInSetlist`, line ~511) and the `offlineSync.js` WRITE_OPS entry stay — replay now goes through the RPC (idempotent; positions recomputed from the ordered ids). **FIRST task of PR#4 — closes the interim-window N-row reorder emissions.** *Refs: S1, S15. Est. ~20. Deps: PR#0–PR#1 applied.*

- [x] 4.2 — `src/lib/setlists.js` `transferOwnership` (line ~659): replace the DELETE/INSERT/flip three-request sequence with `supabase.rpc('transfer_setlist_ownership', { p_setlist_id, p_new_owner_id })`; keep the replay-safe early return and the `guardTransfer` pre-flight (`src/lib/setlistCollab.js` (read-only) stays as-is); queued offline path unchanged (replays through the RPC). *Refs: shared-setlist transfer scenario; zero-'removed' rows. Est. ~25. Deps: PR#0–PR#1 applied.*

- [x] 4.3 — Verification walk (no file changes): two-identity walk (`demo@cemurm.app` ↔ `isolation@cemurm.app`) proving PR#0–PR#1 emissions end-to-end through the client: bandmate invite → invitee row; accept/decline → inviter rows; setlist share/permission/removal → affected-member rows; song add → row with key/tempo; reorder → **exactly one row** naming the moved song (deep link highlights it); transfer → zero 'removed' rows; removed user → zero further rows (row absence). *Refs: I1–I3, S1–S4, Sys1, S15, S10. Est. ~0 (session). Deps: 4.1, 4.2.*

- [x] 4.4 — `src/pages/Notifications.jsx` invitation-row action buttons (render only on `payload.action ∈ {invite, bandmate-accepted, bandmate-declined}`): **View** = `markRead(row)` + navigate `/bandmates`; **Decline** = `declineInvite(userId, bandmateId)` (`src/lib/bandmates.js` (read-only) line ~168) → on success `markRead(row)` — the row is NOT deleted (dismissal = read per the 0002 no-DELETE grant; I3 "dismissed"); **Accept** = `acceptInvite(userId, bandmateId)` (`src/lib/bandmates.js` (read-only) line ~158) → `markRead(row)` + navigate `/bandmates`; keep the existing offline `respondInvite` queue path (bandmates.js lines ~162–176 (read-only)) — a queued response still emits the inviter-side row at drain time. *Refs: I1/I2/I3, bandmates S5. Est. ~65. Deps: 2.4, 4.3.*

- [x] 4.5 — Read-state wiring: page + badge (two `useNotifications` instances) apply `markRead`/`markAllRead` optimistically and let the realtime echo converge the other instance (`postgres_changes` echoes the user's own `read_at` UPDATE — the convergence contract for cross-instance state). *Refs: Feed2, Feed3. Est. ~45. Deps: 2.3, 4.4.*

- [x] 4.6 — Offline recipient side in `src/hooks/useNotifications.js`: cache unread count (`notifications-unread:<userId>`) and last feed (`notifications:<userId>`, read-through) via `offlineGet`/`offlineSet` on `src/lib/offlineCache.js` (read-only — additive keys only; no store/schema/`DB_VERSION` change; additive-upgrade contract untouched); on `online` / `cemurm:sync-done` / `focus` → refetch feed and reconcile badge; synthesize the Off2 summary `{count, byCategory}` from the fetch delta (`unread − lastSeenUnread`, grouped by category) and render it as a banner row in `src/pages/Notifications.jsx`; `consumeSummary()` clears + persists `lastSeenUnread`. *Refs: Off1, Off2. Est. ~90. Deps: 2.3, 4.5.*

- [x] 4.7 — `src/pages/SetlistDetail.jsx` deep-link target: on mount read `?song=<id>` search param → scroll to + highlight the item row via the `data-song-id` selector (song-added / reorder rows); Activity panel stays untouched (two-surface decision). *Refs: Feed6 (song-added/reorder deep links), S15. Est. ~35. Deps: 4.3.*

> PR#4 verification (2026-09-18): `node -e "import('./src/lib/setlists.js').then(m=>m.demo())"` via session-only shim (`/tmp/opencode/setlists-demo-runner.mjs` + `env-shim-loader.mjs` — `setlists.js` is NOT bare-node: its static `import { supabase } from './supabase.js'` reads Vite-only `import.meta.env`, so design.md line 408's bare-node assumption is inaccurate for setlists) → `setlists demo OK: 765s → 12:45 (scenario 4: 3:30+4:00+5:15=12:45)` exit 0 (4 pure-function cases); two-identity psql walk (`walk_pr4_rpc.sql`, session-only in `/tmp/opencode`, exit 0 under `ON_ERROR_STOP`): bandmate invite/accept/decline, share/permission/removal, song-add with `Key: C · 120 BPM`, reorder via `move_setlist_items` → **exactly one row** + actor self-suppressed, transfer via `transfer_setlist_ownership` → zero 'removed' + owner flip + demo re-added, removed user → zero further rows; `pnpm lint` exit 0 (0 warnings); `pnpm build` exit 0 (pre-existing >500 kB chunk warning only). Slice diff vs base `b024872` = **294 changed lines (228 insertions, 66 deletions) ≤ 400 ✓** (useNotifications 78, setlists 78, Notifications.jsx 100, SetlistDetail.jsx 38). Branch `feat/hito-3-notifications-pr4-event-wiring` @ `aa26726` pushed to origin (`3f19ad2` 4.1 + `197ff95` 4.2 + `715bb94` 4.4 + `56a3427` 4.5 + `91d5a23` 4.6 + `aa26726` 4.7 — walk 4.3 is session-only, no commit).

## PR#5 — Comments + @mention (Phase 3–4: Integration / Verification; renumbered from the former PR#4 per RQ #8)
> Verification: `node -e "import('./src/lib/comments.js').then(m=>m.demo())"` (read-only); two-identity comment walk; `pnpm lint && pnpm build`.
> Edit targets: `src/pages/SongDetail.jsx`, `src/pages/Notifications.jsx`. Read-only refs: `src/lib/comments.js`, `src/components/notation/ChordProRenderer.jsx`, `src/lib/auth.js`, `supabase/migrations/0009_notifications_activity.sql` (already merged), `docs/local-dev.md`.

- [x] 5.1 — Verification (no file changes): psql + two-identity walk proving the merged `shared_comments` trigger (1.3, in 0009): comment → owner + accepted collaborators of any setlist containing the song (deduped, minus author/minus mention recipients); `@julian.guitar` in-scope → one mention row; `@marco.tempo` out-of-scope → zero rows; self-mention → suppressed; comment/mention payloads carry `{song_id, setlist_id, section, comment_id}`; mention regex matches the `src/lib/auth.js` (read-only) username charset (align here if signup validation differs — open question). *Refs: comments S11, S12. Est. ~0 (session). Deps: PR#1 (1.3), 4.3.*

- [x] 5.2 — `src/pages/SongDetail.jsx` deep-link target: on mount read `?anchor=<section>&cid=<comment_id>` search params → call the existing `jumpToComment(anchor)` (lines ~324–331) + open/scroll the comments panel and highlight the comment row (`sectionAnchorId` precedent from `src/components/notation/ChordProRenderer.jsx` (read-only)). *Refs: S11/S12 deep link, Feed6. Est. ~30. Deps: 5.1.*

- [x] 5.3 — `src/pages/Notifications.jsx` comment/mention row rendering: comment rows `"{Actor} commented on {Song}"` (action `'comment'`), mention rows `"@{username} mentioned you in {Song}"` (action `'mention'`); tap → `notificationTarget` → `/songs/:song_id?anchor=<section>&cid=<comment_id>` (5.2 target). *Refs: S11, S12. Est. ~75. Deps: 5.1, 5.2.*

- [x] 5.4 — Mention-scope path checks (walk): re-run the @mention scenarios asserting zero rows for out-of-scope names and no 42501 on tap for every emitted mention row (every row must be openable under RLS — the 0006 EXISTS scope is the guard); confirm mention rows are excluded from the comment-recipient set (no double rows). *Refs: comments S12 second scenario, RLS openability. Est. ~0 (session). Deps: 5.1–5.3.*

> PR#5 verification (2026-09-19): `node -e "import('./src/lib/comments.js').then(m=>m.demo())"` → `comments demo OK: 12 asserts (version isolation, thread tree, anchors)` exit 0 + `notifications demo OK: 24 asserts (deep-link map, normalization, grouping)` exit 0 (6.3.1 target covers comment/mention → `/songs/:id?anchor&cid`); two-identity comment walk (`walk_pr5_comments.sql`, session-only in `/tmp/opencode`, exit 0 under `ON_ERROR_STOP`, 11 blocks OK): comment emission (b0 — demo + julian rows, exact payload, author excluded), in-scope `@julian.guitar` mention (b1 — one row, zero double rows), self-mention suppressed (b2), out-of-scope `@outsider` (pending) + unknown `@marco.tempo` → ZERO mention rows (b3), dedup across two membership paths with a second setlist (b4 — julian 1 row across sl1+sl2), 5.4 openability — julian opens his mention row + the commented rows under RLS with NO 42501 (select_self + 0006 scoped select), outsider sees zero out-of-scope rows, global sweep zero duplicate comment_id/recipient rows; mention regex `@[A-Za-z0-9._-]+` aligned with the profiles.username charset — `src/lib/auth.js` NEVER writes usernames (only EMAIL_RE + user_metadata), so design.md's signup-validation open question resolves **by evidence**: no signup charset to differ from (seed + walk usernames `demo`/`isolation`/`outsider`/`julian.guitar` all match the class). `pnpm lint` exit 0 (0 warnings); `pnpm build` exit 0 (pre-existing >500 kB chunk warning only). Slice diff vs base `aa26726` = **56 changed lines (47 insertions, 9 deletions) ≤ 400 ✓** (SongDetail.jsx 49, Notifications.jsx 7). **5.3 reconciled (no functional code change — documented like the comments.js reconciliation in design.md line 127)**: the 0009 trigger bakes the exact `"{Actor} commented on {Song}"` / `"@{username} mentioned you in {Song}"` titles server-side and `notificationTarget` (2.2 data layer, demo-asserted lines 206–213) already routes the row tap to `/songs/:song_id?anchor=<section>&cid=<comment_id>`; the generic row path (2.4) renders them — only the header comment in Notifications.jsx was updated to record the contract. Branch `feat/hito-3-notifications-pr5-comments` @ `c20e66a` pushed to origin (`11a9518` 5.2 + `c20e66a` 5.3 — walks 5.1/5.4 are session-only, no commits). Orchestrator next: open PR#5 (base = pr4 branch `aa26726`); then archive (all 22 IN scenarios now covered: 5.1–5.4 close comments S11/S12 rows).

## IN Scenario → Owning Task Map (22 IN)

| Scenario | Source | Owning task(s) |
|---|---|---|
| Receive invitation notification (View + Decline buttons) | notifications I1 | 0.2, 4.3, 4.4 |
| Accept bandmate invitation via notification | notifications I2 | 0.2, 4.3, 4.4 |
| Decline bandmate invitation via notification (dismiss; inviter notified) | notifications I3 + bandmates S5 | 0.2, 4.3, 4.4 |
| Notification when bandmate reorders (+ offline badge increments) | notifications S1 | 1.1, 1.2, 4.1, 4.3, 4.6 |
| Notification when bandmate adds a song (key/tempo) | notifications S2 | 1.2, 4.3 |
| Notification when permissions change (view-only) | notifications S3 | 0.3, 4.3 |
| Notification when removed + no further notifications | notifications S4 + setlists S10 | 0.4, 4.3 |
| Notification when pending bandmate accepts | notifications Sys1 | 0.2, 4.3 |
| Push-style setlist change notification (in-app row) | setlists S15 | 1.1, 1.2, 4.1, 4.3, 4.7 |
| Comment posted → participating bandmates notified, section deep link | comments S11 | 1.3, 5.1, 5.2, 5.3 |
| @mention notifies the named member (in-scope only) | comments S12 | 1.3, 5.1, 5.3, 5.4 |
| Feed reverse-chron + relative time | notifications Feed1 | 2.1, 2.2, 2.4 |
| Mark single notification read | notifications Feed2 | 2.2, 4.5 |
| Mark all notifications read | notifications Feed3 | 2.2, 4.5 |
| Feed grouped by type with per-group unread counts | notifications Feed4 | 2.2, 2.4 |
| Filter feed by category (All/Invitations/Setlist Changes/Events/System) | notifications Feed5 | 2.4 |
| Deep-link from a notification | notifications Feed6 | 4.7, 5.2 (+ 4.4 navigations) |
| Unread badge on tab icon | notifications Feed7 | 2.5, 4.6 |
| Notifications queued offline, delivered in order on reconnect | notifications Off1 | 4.1, 4.2, 4.6 |
| Offline summary on reconnect ("You have 8 pending notifications") | notifications Off2 | 2.3, 4.6 |
| Subscriber receives only their own rows (non-PK filter + replica identity full) | notifications realtime + row-level-security deltas | 1.4, 1.5(g), 2.2, 2.3 |
| Publication limited to RLS-enabled tables after 0008 | row-level-security delta | 1.4, 1.5(g) |

## Deferred (NOT tasks — honest backlog, out of scope with reason)

| Item | Ref | Disposition |
|---|---|---|
| Web Push infrastructure (SW push, VAPID, OS channels) | notifications push wording | NO infra exists; all "push" wording satisfied as in-app rows (frozen decision 1). Future change. |
| Push preferences ×4 (master switch, granular categories, quiet hours/DND, OS channels) | notifications prefs ×4 | `notification_preferences` stays deny-by-default; 0008/0009 contain zero prefs policy (1.4/1.5 assert the lock). Future change with push. |
| Weekly digest | notifications Sys4/Sys6 | Needs a scheduled aggregation job; no cron infra. |
| Org-membership invitation notification | notifications I4 | `organizations`/`org_memberships` are Hito 4 — not built. |
| Event notifications ×4 (new event, reminder, 1h-before setlist, performance-order update) | notifications event section | `events`/`event_rsvps` not built (Hito 4); reminders need scheduled jobs. Events/System feed tabs render empty states (Feed5) until then. |
| Proximity code expiry notification | notifications Sys2 | Proximity flows deferred from change 1 (bandmates S13); needs `invite_codes` expiry job. |
| Password-reset request notification | notifications Sys3 | Auth-security surface; needs system/auth integration. |
| Feed unification (promoting per-setlist Activity broadcasts to DB rows) | change-1 D5/D6 | Deliberate future change; the two surfaces stay separate (frozen decision 2) — Activity panel untouched. |
| Song-removal emission site (`setlist_items` DELETE) | notifications requirement 2 reading | Stays silent by design ("adds, or removes content" read as member-removal); new trigger + scenario needed if product wants it later. |
| Retention / purge RPC (delete `read_at IS NOT NULL` rows older than N months) | feed growth | Noted for later; no cleanup job in 0001/0009. |
| Emitter-side realtime pending badge | notifications Off1 wording | Already satisfied by the existing `pendingSync` pattern; no new queue or op names (design: `offlineQueue.js`/`offlineSync.js` unchanged). |